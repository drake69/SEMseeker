# Detect whether a data frame is in coordinate format (CHR + START columns).

Detect whether a data frame is in coordinate format (CHR + START
columns).

## Usage

``` r
io_is_coord_format(df)
```

## Arguments

- df:

  A data frame.

## Value

Logical scalar.
