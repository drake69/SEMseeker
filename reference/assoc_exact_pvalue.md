# Quantile regression result value, confidence interval and p.value

Quantile regression result value, confidence interval and p.value

## Usage

``` r
assoc_exact_pvalue(permutation_vector, statistic_parameter, conf.level)
```

## Arguments

- permutation_vector:

  vector of permutation statistc signal regression

- statistic_parameter:

  signal regression

- conf.level:

  confidence intervals alpha level

## Value

ci and p.value and pvalue_limit
